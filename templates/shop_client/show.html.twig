{% extends 'base.html.twig' %}

{% block title %}ShopClient{% endblock %}

{% block body %}
    <h1>ShopClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop_client.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shop_client.Name }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ shop_client.LastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shop_client_index') }}">back to list</a>

    <a href="{{ path('shop_client_edit', {'id': shop_client.id}) }}">edit</a>

    {{ include('shop_client/_delete_form.html.twig') }}
{% endblock %}
